# Overleaf Git token. Generate/revoke at:
#   Overleaf -> Account Settings -> Git Integration
# Copy this file to .env (gitignored) and put YOUR token in it. Never commit the real token.
OVERLEAF_GIT_TOKEN=olp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
